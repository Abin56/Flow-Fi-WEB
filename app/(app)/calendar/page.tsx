import { Calendar } from "lucide-react";
import { FeaturePlaceholder } from "@/components/layout/feature-placeholder";

export default function CalendarPage() {
  return (
    <FeaturePlaceholder
      icon={Calendar}
      title="Calendar"
      description="Bills, EMIs, income and upcoming payments on a calendar view — coming in a later pass."
    />
  );
}
